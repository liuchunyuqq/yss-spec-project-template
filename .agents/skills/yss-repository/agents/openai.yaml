version: 1
interface:
  display_name: "YSS Repository"
  short_description: "Infrastructure Repository、Mapper、Convertor 与端口实现规范"
  default_prompt: "Use $yss-repository to implement Infrastructure persistence and query adapters against existing Domain Gateway and Application Query Port interfaces without creating domain contracts."

version: 1
interface:
  display_name: "YSS DDD Scaffold"
  short_description: "生成多模块 YSS DDD 后端脚手架与工程骨架规范"
  default_prompt: "Use $yss-ddd-scaffold-generator only with a lifecycle-approved scaffold contract JSON to create a pure YSS DDD backend skeleton with domain, application, infrastructure, adapter, and bootstrap module boundaries; route all business code back through yss-implementation-contract-compiler and the applicable YSS skills."

---
pipeline: <feature-id>
stage: open
status: ready-for-human
owner: ai
---

# 产品研发规格：<功能名称>

## 功能父 Ticket

<Local：`docs/.scratch/<feature>/parent-ticket.md`；GitHub / GitLab：Issue URL / 编号>

## 问题陈述

（从用户视角描述要解决的问题。）

## 解决方案

（从用户视角描述解决方案。）

## 用户故事

1. 作为<角色>，我希望<能力>，以便<收益>。

## 功能需求

| ID | 需求 | 优先级 | 备注 |
|---|---|---|---|
| FR-001 | | P0/P1/P2 | |

## 非功能需求

| 类别 | 需求 | 目标 |
|---|---|---|
| 性能 | | |
| 可靠性 | | |

## 验收标准

```gherkin
假如 <上下文>
当 <操作>
那么 <可观察结果>
```

## 产品总体设计 / 功能架构

- 功能域与模块边界：
- MVP / 非目标：
- UI 影响：是 / 否
- 产品总体设计：`docs/.scratch/<feature>/design/<feature>-product-overview-design.md`

> 只有存在产品设计影响时，才强制低保真页面草图、状态矩阵、按 H1/H2 路由的原型交付物和用户确认；否则记录 `not-applicable` 及原因。

## OpenAPI 影响

- [ ] 无
- [ ] 需要 API 影响分析 / 契约草案
- [ ] 需要 review-only OpenAPI Draft：`docs/.scratch/<feature>/api/<feature>.yaml`

> OpenAPI Draft 在 OpenAPI Freeze 前仅用于评审和架构反审，不作为实现或生成客户端的稳定契约。

受影响端点：

| 方法 | 路径 | 变更 |
|---|---|---|
| | | |

## DDD 影响判断

> 仅做轻量判断，不新增流程阶段。若均为“否”，后续不需要单独 DDD 设计；若触发边界、模型或一致性变化，在产品总体设计 / 系统概要设计中补齐对应小节。

| 检查项 | 是 / 否 | 说明 / 处理 |
|---|---|---|
| 是否新增或改变统一语言 / 核心术语 |  | 需要回写 `CONTEXT.md` 的中文术语和 PascalCase `英文标识` |
| 是否新增或改变限界上下文 / 模块边界 |  | 需要在产品总体设计中完成 Strategic DDD Check |
| 是否改变上下文间协作关系 |  | 说明上游 / 下游 / 防腐层 / 共享内核关系 |
| 是否新增或改变聚合、不变量、状态机 |  | 需要在系统概要设计 / 数据架构中完成 Tactical DDD Check |
| 是否影响 API、权限、数据、审计或风险 / 回滚约束 |  | 进入对应契约、架构或人工审查门禁 |

## 测试决策

- 主要测试 seam：
- 代码库中的相似测试：
- 必需测试：
  - [ ] 单元 / 领域行为
  - [ ] API / 契约
  - [ ] 前端组件
  - [ ] E2E 关键路径

## AI / 人工审查点

| 范围 | Agent 权限 | 是否需要人工审查 | 备注 |
|---|---|---|---|
| 支付逻辑 | 记录验证证据 | 是 | |
| 原生 SQL | 记录验证证据 | 是 | |
| 公共基础库 API | 记录验证证据 | 是 | |

## 非目标范围

## 风险

version: 1
interface:
  display_name: "YSS Domain"
  short_description: "按批准战术模型实现聚合、规则与 Domain Gateway 接口"
  default_prompt: "Use $yss-domain to implement approved aggregates, entities, business rules, domain tests, and the sole Domain Gateway interfaces; reroute instead of inventing unresolved rules."
